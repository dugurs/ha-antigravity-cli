run_command:
  name: Run Command
  description: Executes a command via the Antigravity CLI agent.
  fields:
    command:
      name: Command
      description: The command string to execute.
      required: true
      example: "echo 'hello'"
      selector:
        text:
